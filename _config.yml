# ============================================
# MYCELIUM GRID — Jekyll Configuration
# ============================================

# -- Site Identity --
title: "Zöe"
tagline: "Aspring IAM Architect.  Explorer of National Parks, Cybersecurity, and Critical Theory."
description: "Personal site — projects, writings, and the space between."
baseurl: ""         # Leave empty if deploying to username.github.io
url: "https://yourusername.github.io"

# -- Author --
author:
  name: "Your Name"
  email: "you@example.com"

# -- Social (optional — leave blank to hide) --
github_username:   "yourusername"
linkedin_username: ""

# -- Navigation brand text --
nav_brand: "~/Zöe R"

# -- Home page options --
show_about_on_home: true
about_blurb: |
  Hi, I'm Zöe.  
  I build communities, learn voraciously, and love nature 🌲🏔️🦌🌿🏕️.  
  In this space you'll find my musings, projects, and random thoughts.

# -- Footer --
footer_tagline: "Staying grounded.  Looking towards the stars.  Eyes on the horizon of endless possibilities."

# -- Build settings --
markdown: kramdown
highlighter: rouge
permalink: /blog/:year/:month/:day/:title/

kramdown:
  input: GFM
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    css_class: 'highlight'

# -- Plugins --
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap

# -- Collections --
collections:
  projects:
    output: true
    permalink: /projects/:name/

# -- Defaults --
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
      type: "projects"
    values:
      layout: "page"
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "page"

# -- Exclude from build --
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor
  - README.md
  - "*.sh"
